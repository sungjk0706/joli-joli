# Joli-Joli 프로젝트 인계서 (디자인 일관성 확보 완료)

## 📅 세션 작업 요약 (2026-05-11)

### 1. 스마트폰(Mobile) 메인 버튼 디자인 일관성 확보
- **PC 버전 디자인 이식**: `CustomerViewPhone.jsx`의 메인 버튼 4개를 PC 버전(`CustomerViewDesktop.jsx`)의 색상과 스타일로 전면 교체.
- **불필요한 요소 제거**: 이전 세션의 'Alive' 애니메이션, 쉬머 효과, 레드 도트 등을 제거하여 PC의 정갈한 디자인 언어와 통일.
- **코드 최적화**: 사용되지 않는 `Button` 컴포넌트 임포트 제거 및 기본 HTML `button` 태그로 전환.

### 2. 관리자 로그인 버튼 노출 로직 수정
- **라이브 모드 감지**: `CustomerView.jsx`의 로컬 라이브 상태를 전역 `useLiveStore`에 동기화.
- **버튼 자동 숨김**: `App.jsx`에서 라이브 영상 시청 중(전체화면)에는 관리자 로그인 버튼이 나타나지 않도록 수정하여 시청 방해 요소 제거. (미니모드 시에는 다시 노출)

### 3. 주요 파일 변경 사항
- `CustomerViewPhone.jsx`: 메인 버튼 디자인 및 텍스트 수정.
- `App.jsx`: `useLiveStore` 구독 및 관리자 버튼 노출 조건 강화.
- `CustomerView.jsx`: 라이브/미니모드 상태 전역 스토어 동기화 로직 추가.

## 🚀 다음 세션 작업 계획 (Tablet & PC)

### 1. 대화면 최적화
- **기기별 뷰 업데이트**: `CustomerViewTablet.jsx`, `CustomerViewDesktop.jsx`에 완성된 디자인 언어 재검토 및 고도화.
- **레이아웃 확장**: 큰 화면에서는 버튼들을 그리드 형태로 더 넓게 배치하거나, 사이드 바를 활용하는 등 'Bento Grid' 스타일의 레이아웃 검토.

### 2. 컴포넌트 일관성 유지
- 스마트폰에서 성공적으로 적용된 PC 스타일의 버튼 디자인을 전체 기기에 일관되게 유지.
- 관리자 버튼(`Sparkles` 아이콘)의 디자인 톤도 메인 버튼들과 조화로운지 재점검.

---
**주의**: 인스타그램 연결 시 크롬 잔상을 피하기 위해 반드시 `window.location.href` 방식을 유지할 것.
